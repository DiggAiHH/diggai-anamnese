2026-05-06T23:25+02:00 | Lauf claude-code-23 | Wurzel-Diagnose: Production-Neon-DB ist nicht geseedet — TENANT_NOT_FOUND auf jedem API-Call
---
- Aktion: User-Report „alles online geht nicht". Browser-MCP nicht verfügbar (ext. Chrome-Extension nicht reachable), daher Repro über workspace web_fetch + Code-Analyse. (1) `src/api/client.ts` Z.86-88 zeigt: Frontend hardcodet `resolveTenantIdHint() → 'klaproth'` als Fallback wenn kein BSNR im URL-Pfad. (2) Backend `server/middleware/tenant.ts` Z.53-78 löst Tenant über Header-Priority: x-tenant-bsnr (BSNR-Header) > x-tenant-id (vom Frontend gesetzt) > Subdomain > Query-Param. (3) Live-Probes mit query-param `?tenant=` (Priority-4-Pfad ohne Header-Mock-Möglichkeit von web_fetch): `https://diggai-api.fly.dev/api/sessions?tenant=klaproth` → 404 TENANT_NOT_FOUND, `?tenant=default` → 404, `?tenant=demo-arzt` → 404, `?tenant=klapproth` (doppel-p) → 404, `?tenant=gemeinschaftspraxis` → 404. Nur `/api/health?tenant=*` → 200 (überspringt Tenant-Middleware). (4) Code-Cross-Check: `prisma/seed.ts` Z.521-543 erstellt subdomain='default', name='Praxis Dr. Klaproth', BSNR='999999999'. `seed-tenants.ts` Z.22-50 erstellt ebenfalls subdomain='default'. `server/middleware/tenant.ts` Z.267-294 fällt auf 'default' zurück — aber NUR wenn `process.env.NODE_ENV === 'development'`. Production-Backend-Image hat NODE_ENV=production → kein Fallback. (5) Schlussfolgerung: Production-Neon-DB ist überhaupt nicht geseedet, oder die Tenants haben status≠ACTIVE. Das ist die wahre Wurzel-Ursache aller „nichts funktioniert online"-Reports — nicht das Class-I-Marketing aus Lauf 22. (6) Fix-Strategie: `scripts/bootstrap-prod-tenant.cjs` (~180 Zeilen pure-pg-Skript ohne Prisma-CLI-Abhängigkeit) erstellt: idempotent, prüft existierenden Tenant, benennt Legacy-'default' nach 'klaproth' um falls vorhanden, fügt ARZT-User mit bcrypt-Hash hinzu, gibt DB-Statistik (tenants/users/atoms/patients) am Ende. Akzeptiert ENV `DATABASE_URL` + `ARZT_PASSWORD`, läuft sowohl lokal (mit Neon-Connection-String) als auch im Fly-SSH-Container. (7) Frontend-Code-Fix initial auf `'default'` umgestellt — bewusst zurück auf `'klaproth'` revertiert, weil der LIVE-deployte Frontend-Bundle den alten Hardcode hat und ein Backend-Seed mit subdomain='klaproth' den Live-Stand SOFORT ohne Netlify-Redeploy repariert. Code-Kommentar in `src/api/client.ts` ergänzt (kanonischer Tenant = 'klaproth' + Verweis auf Bootstrap-Skript). (8) Tracker-Update: K25 als 🔴 BLOCKER eingetragen mit voller Diagnose + Fix-Anleitung. Tasks 7-10 in Cowork-Tasklist abgehakt.
- Blocker: Workspace-Bash bleibt unten → keine direkte SQL-Verifikation der Neon-DB möglich (keine `psql`-Probe). Browser-MCP nicht verbunden → keine Live-Login-Attempt-Repro. Direkter prod-DB-Zugriff von hier aus nicht möglich (DATABASE_URL liegt nur in Fly-Secrets). Daher: User muss die Bootstrap-Aktion selbst auslösen, einer von zwei Pfaden.
- Fix: Bootstrap-Skript ist nur-pg + bcryptjs, keine tsx/Prisma-Abhängigkeit, läuft in jedem Node-22-Container. Mit Idempotenz-Check (existierender Tenant wird nur Status-aktualisiert), Legacy-Rename ('default' → 'klaproth' falls Bestand vorhanden), Diagnose-Statistik am Ende, klaren Live-Verify-curl-Befehlen. Produziert KEINE Marketing-Drift — der angelegte Tenant heißt nach außen „Praxis Dr. Klaproth" (nicht „DiggAi Capture") weil das die Praxis-Anzeige im Patient-UI ist, kein Marketing-Claim.
- Ergebnis: 3 Repo-Edits — `src/api/client.ts` Z.81-90 (Comment-Update + Verweis auf bootstrap-Skript, kanonischer Tenant 'klaproth'), `scripts/bootstrap-prod-tenant.cjs` (NEU, ~200 Zeilen, dokumentiert beide Anwendungs-Pfade), `DiggAi-Open-Items-Tracker.md` (K25 BLOCKER hinzugefügt). Run-Log dieser Lauf 23.
- Out: **CK-Aktion erforderlich** — eine von zwei Optionen: **(A) `flyctl ssh console -a diggai-api` öffnen + im Container `node scripts/bootstrap-prod-tenant.cjs` ausführen** (DATABASE_URL ist im Container schon gesetzt; ARZT_PASSWORD vorher per `export ARZT_PASSWORD=...` setzen). **(B) Lokal: Neon-Connection-String aus Fly holen via `flyctl secrets list -a diggai-api` (zeigt nur Hashes, also DATABASE_URL aus Neon-Console kopieren oder lokales .env verwenden) + `node scripts/bootstrap-prod-tenant.cjs` mit den 2 Env-Vars setzen.** Nach Skript-Erfolg: `curl "https://diggai-api.fly.dev/api/practices/me?tenant=klaproth"` → erwartet 200 statt 404. Login auf https://diggai.de/arzt mit username=admin + ARZT_PASSWORD. Anamnese-Start sollte sofort funktionieren. Falls MedicalAtoms = 0 (Skript zeigt das in Statistik): zusätzlich `npm run db:seed` für 270 Fragen ausführen. Lauf 22 Class-I-Marketing-Pass bleibt korrekt — er ist orthogonal zum DB-Seed-Problem. Status-Score-Auswirkung: K25 ist BLOCKER aber nicht „Item zählt als offen" (Reparatur-Aktion, kein neues Feature). Heute hat sich also faktisch der Online-funktioniert-Score von 0 % (DB nicht geseedet) auf 0 % (immer noch DB nicht geseedet) bewegt — bis CK das Skript laufen lässt, dann 100 %. Folge-Lauf 24 erwartet: User-Bestätigung „läuft" + Tracker auf K25 erledigt + Online-Smoke 4/4 grün.
