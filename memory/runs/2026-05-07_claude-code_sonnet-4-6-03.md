2026-05-07T02:00+02:00 | Lauf claude-code-sonnet-03 | Login-Smoke-Test + DNS-Blockade + Regulatorik-Plan
---
- Aktion: (1) Continuation nach Lauf 02 (Fly reaktiviert, Bootstrap gefixt). Ziel: Browser-Login-Smoke-Test https://diggai.de/arzt. (2) form_input + button.click via Chrome-MCP → kein API-Call (React synth. Events nicht getriggert). (3) nativeInputValueSetter + dispatchEvent('input') → React-State OK, aber btn.click() aus JS traf isPending=true (Mutation aus vorherigem Versuch noch in-flight). (4) Echte Tastatureingabe via triple_click+type → React-State korrekt gesetzt, Login-Button spinnt. (5) Health-Check `fetch('https://diggai-api.fly.dev/api/health')` → "Failed to fetch". (6) `nslookup diggai-api.fly.dev` + `ping` auf Windows → "Host nicht gefunden", DNS-Server 192.168.2.1 (lokaler Router) timeout. (7) `nslookup google.com 8.8.8.8` → timeout (UDP port 53 outbound blockiert oder Router down). (8) Chrome-Tab direkt zu `diggai-api.fly.dev/api/health` → Error page. Diagnose: lokale DNS-Auflösung für *.fly.dev vollständig ausgefallen, kein Backend-Bug. (9) Bundle-Check: `diggai-api.fly.dev/api` korrekt in der deployed JS baked. VITE_API_URL korrekt in netlify.toml. (10) Tracker-Update: K20 → erledigt, K26 → erledigt, K27 neu (Login DNS-Block). (11) Regulatorik-Plan als `DiggAi-Regulatory-Flip-Plan.md` im Workspace-Root.
- Blocker: Lokaler DNS-Ausfall — Router 192.168.2.1 antwortet nicht auf DNS-Queries. fly.dev + api.fly.io + google.com alle nicht auflösbar. Chrome DoH ebenfalls ausgefallen. Login-Smoke-Test nicht durchführbar von dieser Maschine.
- Fix: Kein Code-Fix nötig. Login-Smoke von mobilem Gerät / Hotspot durchführen. Alternativ: Router neu starten oder DNS in Netzwerkadapter auf 1.1.1.1 umstellen.
- Ergebnis: Tracker K20 + K26 erledigt. K27 neu dokumentiert. Run-Log 03 geschrieben. Regulatorik-Plan erstellt.
- Out: Backend war healthy (Lauf 02 verifiziert). Login-Code korrekt (diggai-api.fly.dev/api baked). Lokal nicht testbar wegen DNS. Nächste Aktion CK: (1) Router neu starten → Login-Smoke via Handy oder nach DNS-Fix. (2) A4/A5 Gmail-Drafts senden. (3) Regulatorik-Plan lesen → Self-Certification-Checkliste abarbeiten.
