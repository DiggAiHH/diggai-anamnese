[build]
  command = "npm run build"
  publish = "dist"
  # Backend runs on Railway — Netlify Functions not used
  functions = "netlify/no-functions-placeholder"

[build.environment]
  NODE_VERSION = "22"
  NETLIFY_SITE_ID = "4e24807c-6ea8-482e-8bef-6c688f7172bb"
  # 2026-05-06: Build OOM bei Default 2GB — vite+terser+rolldown brauchen mehr.
  NODE_OPTIONS = "--max-old-space-size=4096"
  # VITE_API_URL is injected per-context below.
  # Override in Netlify Dashboard > Site configuration > Environment variables
  # if you want to test a branch against a specific backend.

# ─── Production context (main branch only) ──────────────────────────────────
[context.production]
  command = "npm run build"

[context.production.environment]
  # Production backend endpoint
  # 2026-05-06: Migration von Hetzner (TLS broken) auf Fly.io Frankfurt.
  # api.diggai.de zeigt aktuell noch auf den toten Hetzner-VPS — sobald DNS
  # auf Fly umgestellt ist (A→66.241.125.72), kann das wieder api.diggai.de sein.
  VITE_API_URL = "https://diggai-api.fly.dev/api"
  VITE_SOCKET_URL = "https://diggai-api.fly.dev"
  VITE_DISABLE_DEMO_MODE = "false"

# ─── Branch deploy context (disabled in Dashboard — defined here for safety) ─
# To disable branch deploys: Netlify Dashboard > Site configuration >
#   Build & deploy > Branches and deploy contexts > Branch deploys = "None"
[context.branch-deploy]
  command = "echo 'Branch deploys are disabled for production.' && exit 1"

# ─── DSGVO/BSI Security Headers ─────────────────────────────────────────────
[[headers]]
  for = "/*"
  [headers.values]
    # HSTS — BSI TR-02102: 1 year, includeSubDomains, preload
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    # Kamera + Mikrofon für Videosprechstunde (self), rest gesperrt
    Permissions-Policy = "camera=(self), microphone=(self), geolocation=(), payment=()"
    # CSP — unsafe-eval required for lottie-web animation expressions
    # K23 2026-05-07: api-takios.diggai.de (deprecated Hetzner) entfernt — DNS cutover Lauf 13 bestätigt.
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob:; media-src 'self' blob:; connect-src 'self' https://*.netlify.app wss://*.netlify.app https://*.diggai.de wss://*.diggai.de https://api.diggai.de wss://api.diggai.de https://diggai-api.fly.dev wss://diggai-api.fly.dev https://*.fly.dev wss://*.fly.dev https://*.up.railway.app wss://*.up.railway.app https://*.onrender.com wss://*.onrender.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self';"
    # XSS-Protection off — CSP handles this; old header can trigger bugs
    X-XSS-Protection = "0"
    Cross-Origin-Embedder-Policy = "credentialless"
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Resource-Policy = "same-origin"

# Immutable assets — hashed filenames, 1-year cache
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Hatami subsite assets — static SPA under /hatami
[[headers]]
  for = "/hatami/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Service Worker — never cache (must always be fresh for updates)
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Service-Worker-Allowed = "/"

[[headers]]
  for = "/hatami/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Service-Worker-Allowed = "/hatami/"

# SPA entry point — no cache (gets latest index.html on every load)
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/hatami/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# Locale JSON files — short-lived cache (10 min) to allow i18n hotfixes
[[headers]]
  for = "/locales/*"
  [headers.values]
    Cache-Control = "public, max-age=600, stale-while-revalidate=86400"

[[headers]]
  for = "/hatami/locales/*"
  [headers.values]
    Cache-Control = "public, max-age=600, stale-while-revalidate=86400"

# Hatami subsite SPA fallback — keep diggai.de root app untouched
[[redirects]]
  from = "/hatami"
  to = "/hatami/index.html"
  status = 200

[[redirects]]
  from = "/hatami/*"
  to = "/hatami/index.html"
  status = 200

# SPA fallback — all unmatched paths serve index.html
# This covers BSNR-prefixed routes such as:
#   /123456789          → BsnrLayout (index = HomeScreen)
#   /123456789/anamnese → BsnrLayout > AnamnesePage
#   /123456789/patient  → BsnrLayout > PatientApp
# React Router handles client-side routing after index.html is served.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
