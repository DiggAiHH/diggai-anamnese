{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "description": "Browser automation for E2E testing — 22 Playwright specs in e2e/"
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "D:\\Klaproth Projekte\\DiggAi\\Ananmese\\diggai-anamnese-master"
      ],
      "description": "File system access — scoped to the active DiggAI workspace only."
    },
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "${DATABASE_URL}"
      ],
      "description": "Direct PostgreSQL queries for schema inspection and debugging. Requires DATABASE_URL env var."
    }
  }
}
